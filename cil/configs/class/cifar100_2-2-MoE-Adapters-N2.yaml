# CIFAR-100 even split: 2 classes per task (50 tasks). N=2 experts, MoE only.
defaults:
  - uneven_cifar100/cifar100_base
  - _self_

initial_increment: 2
increment: 2
method: "MoE-Adapters"
# N=2, top_k=2 set via model (optional; base does not set num_experts)
