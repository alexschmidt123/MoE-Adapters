# Configuration for CIFAR-100: 10-10 scenario, N=8, Original, NoGNN
# Generated unified config for comprehensive experiments

defaults:
  - reference/cifar100_10-10-MoE-Adapters
  - _self_

# Override method name to create distinct output directory
method: "MoE-Adapters-N8"

# Dataset configuration
dataset_root: ""
class_order: ""
workdir: ""
scenario: "class"
prompt_template: "a bad photo of a {}."
log_path: "metrics.json"
model_name: "ViT-B/16"
dataset: "cifar100"
batch_size: 128
increment: ${initial_increment}
initial_increment: 10
weight_decay: 0.0
l2: 0
ce_method: 0
lr: 1e-3
ls: 0.0

# Model configuration
model:
  # MoE parameters
  num_experts: 8           # Number of experts (N=8)
  top_k: 2                 # Keep k=2 (standard sparse MoE)
  
  # Standard MoE (no HMoE)
  hmoe_enabled: false
  
  # Graph mixer DISABLED
  graph_mixer_enabled: false
